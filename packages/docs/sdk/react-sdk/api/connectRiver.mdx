---
title: connectRiver
---
Connect to River using a SignerContext

Useful for server side code: you can persist the signer context and use it to auth with River later

## Imports

```ts
import { connectRiver } from '@towns-protocol/react-sdk'
```

## Definition

```ts
function connectRiver(
  signerContext: SignerContext,
  config: Omit<SyncAgentConfig, 'context'>,
): Promise<SyncAgent>
```

**Source:** [connectRiver](https://github.com/towns-protocol/towns/blob/main/packages/react-sdk/src/connectRiver.ts)

## Parameters

### signerContext

- **Type:** `SignerContext`

The signer context to use

### config

- **Type:** `Omit<SyncAgentConfig, 'context'>`

The configuration for the sync agent

## Return Type

The sync agent

```ts
Promise<SyncAgent>
```
