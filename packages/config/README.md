# @towns-protocol/config

Here you will find the shared configuration for the Towns Protocol monorepo.

- tsconfig.json
- vite package build config
- eslint config
- prettier config
- typedoc config in the future?

# TODO

make this an actual package :3
