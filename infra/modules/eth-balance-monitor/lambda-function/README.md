## Generating metric artifacts locally

1. Set the following environment variables:

   - BASE_CHAIN_RPC_URL
   - RIVER_CHAIN_RPC_URL
   - ENVIRONMENT (e.g `gamma`, `omega`)

2. Run `yarn artifacts`

3. Check your `artifacts` folder for the generated artifacts
