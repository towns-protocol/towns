version: '3'
services:
  terraform:
    # build:
    #   context: .
    image: herenotthere/hnt-infra:latest
    volumes:
    - .:/mnt/
    - ~/.aws/:/root/.aws
    working_dir: /mnt
    env_file: .env
