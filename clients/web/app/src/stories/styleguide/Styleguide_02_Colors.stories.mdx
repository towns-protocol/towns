import { Meta, Canvas, Story } from "@storybook/addon-docs";
import {
  Avatar,
  Box,
  Grid,
  Text,
  Paragraph,
  Heading,
} from "../../ui/components";
import { vars } from "../../ui/styles/vars.css";
import { Row, Square } from "../StoryUtils";

<Meta title="Style Guide/Colors" />

# Colors

## Background Fills

<Box>
  {Object.keys(vars.color.background).map((c, i) => (
    <Row key={c} label={c}>
      <Square background={c} border="regular" />
    </Row>
  ))}
</Box>

## Foreground

<Box>
  {Object.keys(vars.color.text).map((c, i) => (
    <Row key={c} label={c}>
      <Box direction="row" alignItems="center" gap="sm">
        <Square color={c} background="transparent" border="regular" />
        <Paragraph color={c}>0123456789</Paragraph>
      </Box>
    </Row>
  ))}
</Box>
