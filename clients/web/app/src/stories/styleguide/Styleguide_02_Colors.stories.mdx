import { Meta } from "@storybook/addon-docs";
import { vars } from "../../ui/styles/vars.css";
import { Box, Icon, Paragraph } from "../../ui/components";
import { Row } from "../StoryUtils";

<Meta title="Style Guide/Colors" />

# Colors

## Background Fills

<Box>
  {Object.keys(vars.color.layer).map((c) => (
    <Row key={c} label={c} invert={c.match(/overlay/)}>
      <Box padding background={c}>
        <Paragraph>Default text color</Paragraph>
      </Box>
    </Row>
  ))}
</Box>

## Foreground

<Box>
  {Object.keys(vars.color.text).map((c) => (
    <Row
      key={c}
      label={c}
      backgroundVariant={
        c.match(/inverted/)
          ? "inverted"
          : c.match(/onSemantic/)
          ? "accent"
          : undefined
      }
    >
      <Box direction="row" alignItems="center" gap="sm">
        <Icon color={c} />
        <Paragraph color={c}>Text color</Paragraph>
      </Box>
    </Row>
  ))}
</Box>

## Semantics (placeholders ⚠️)

<Box>
  {Object.keys(vars.color.semantic).map((c) => (
    <Row key={c} label={c} invert={c.match(/overlay/)}>
      <Box padding background={c}>
        <Paragraph>Default text color</Paragraph>
      </Box>
    </Row>
  ))}
</Box>
