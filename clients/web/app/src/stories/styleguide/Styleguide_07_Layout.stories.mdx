import { Meta, Canvas, Story } from "@storybook/addon-docs";
import {
  Avatar,
  Box,
  Grid,
  Text,
  Paragraph,
  Heading,
} from "../../ui/components";
import { vars } from "../../ui/styles/vars.css";
import {
  flexAlignment,
  flexJustifyAlignment,
} from "../../ui/components/Box/Box.css";
import { Row, Square } from "../StoryUtils";

<Meta title="Style Guide/Layout" />

# Box Layout

## Box / Direction

<Row label="Column (default)">
  <Box border="strong" padding="sm" gap="md" direction="column">
    <Square />
    <Square />
    <Square />
  </Box>
</Row>

<Row label="Row">
  <Box border="strong" padding="sm" gap="md" direction="row">
    <Square />
    <Square />
    <Square />
  </Box>
</Row>

## Box / Gap

<Box>
  {Object.keys(vars.space).map((c) => (
    <Row key={c} label={c}>
      <Box direction="row" border="strong" padding="sm" gap={c}>
        <Square />
        <Square />
        <Square />
      </Box>
    </Row>
  ))}
</Box>

## Box / Grow

<Row
  label={
    <>
      <p>Grow shorthand</p>
      <code>{`<Box grow /><Box grow />`}</code>
    </>
  }
>
  <Box gap="md">
    <Box direction="row" border="strong">
      <Box padding="sm" borderRight="strong" grow></Box>
      <Box padding="sm" centerContent={true}></Box>
    </Box>
  </Box>
</Row>
<Row
  label={
    <>
      <p>Grow equally</p>
      <code>{`<Box grow /><Box grow />`}</code>
    </>
  }
>
  <Box gap="md">
    <Box direction="row" border="strong">
      <Box grow padding="sm" borderRight="strong"></Box>
      <Box grow padding="sm" centerContent={true}></Box>
    </Box>
  </Box>
</Row>
<Row
  label={
    <>
      <p>Fraction 5/2 </p>
      <code>{`<Box grow="x5" /><Box grow="x2" />`}</code>
    </>
  }
>
  <Box gap="md">
    <Box direction="row" border="strong">
      <Box grow={"x4"} padding="sm" borderRight="strong"></Box>
      <Box grow={"x2"} padding="sm" centerContent={true}></Box>
    </Box>
  </Box>
</Row>

## Box / justifyContent

### (Row axis)

<Box>
  {Object.keys(flexJustifyAlignment).map((c, i) => (
    <Row key={c} label={c}>
      <Box
        padding="sm"
        gap="sm"
        aspectRatio="3/1"
        direction="row"
        border="strong"
        justifyContent={c}
      >
        <Square />
        <Square />
      </Box>
    </Row>
  ))}
</Box>

## Box / alignItems

### (Row axis)

<Box>
  {Object.keys(flexAlignment).map((c, i) => (
    <Row key={c} label={c}>
      <Box
        padding="sm"
        gap="sm"
        direction="row"
        aspectRatio="3/1"
        border="strong"
        alignItems={c}
      >
        <Square />
        <Square />
      </Box>
    </Row>
  ))}
</Box>
