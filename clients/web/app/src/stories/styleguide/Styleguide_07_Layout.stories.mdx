import { Meta } from "@storybook/addon-docs";
import { Box } from "../../ui/components";
import { vars } from "../../ui/styles/vars.css";

import {
  flexAlignment,
  flexJustifyAlignment,
} from "../../ui/styles/atoms/properties/boxProperties.css";
import { Row, Square } from "../StoryUtils";

<Meta title="Style Guide/Layout" />

# Box Layout

## Box / Direction

<Row label="Column (default)">
  <Box border="strong" padding="md" gap="lg" direction="column">
    <Square />
    <Square />
    <Square />
  </Box>
</Row>

<Row label="Row">
  <Box border="strong" padding="md" gap="lg" direction="row">
    <Square />
    <Square />
    <Square />
  </Box>
</Row>

## Box / Gap

<Box>
  {Object.keys(vars.space).map((c) => (
    <Row key={c} label={c}>
      <Box direction="row" border="strong" padding="md" gap={c}>
        <Square />
        <Square />
        <Square />
      </Box>
    </Row>
  ))}
</Box>

## Box / Grow

<Row
  label={
    <>
      <p>Grow shorthand</p>
      <code>{`<Box grow /><Box grow />`}</code>
    </>
  }
>
  <Box gap="lg">
    <Box direction="row" border="strong">
      <Box grow padding="md" borderRight="strong" />
      <Box centerContent padding="md" />
    </Box>
  </Box>
</Row>
<Row
  label={
    <>
      <p>Grow equally</p>
      <code>{`<Box grow /><Box grow />`}</code>
    </>
  }
>
  <Box gap="lg">
    <Box direction="row" border="strong">
      <Box grow padding="md" borderRight="strong" />
      <Box grow centerContent padding="md" />
    </Box>
  </Box>
</Row>
<Row
  label={
    <>
      <p>Fraction 5/2 </p>
      <code>{`<Box grow="x5"><Box grow="x2" />`}</code>
    </>
  }
>
  <Box gap="lg">
    <Box direction="row" border="strong">
      <Box grow="x4" padding="md" borderRight="strong">
        b1
      </Box>
      <Box centerContent grow="x2" padding="md">
        b2
      </Box>
    </Box>
  </Box>
</Row>

## Box / justifyContent

### (Row axis)

<Box>
  {Object.keys(flexJustifyAlignment).map((c) => (
    <Row key={c} label={c}>
      <Box
        padding="md"
        gap="md"
        aspectRatio="3/1"
        direction="row"
        border="strong"
        justifyContent={c}
      >
        <Square />
        <Square />
      </Box>
    </Row>
  ))}
</Box>

## Box / alignItems

### (Row axis)

<Box>
  {Object.keys(flexAlignment).map((c) => (
    <Row key={c} label={c}>
      <Box
        padding="md"
        gap="md"
        direction="row"
        aspectRatio="3/1"
        border="strong"
        alignItems={c}
      >
        <Square />
        <Square />
      </Box>
    </Row>
  ))}
</Box>
