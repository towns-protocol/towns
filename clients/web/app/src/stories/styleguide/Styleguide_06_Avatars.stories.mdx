import { Meta } from "@storybook/addon-docs";
import { Avatar, Box } from "../../ui/components";
import { avatarSizes } from "../../ui/styles/atoms/properties/avatarProperties.css";
import { Row } from "../StoryUtils";

<Meta title="Style Guide/Avatars" />

# Avatars

## Sizes

<Box>
  {Object.keys(avatarSizes).map((c) => (
    <Row key={c} label={c}>
      <Avatar size={c} />
    </Row>
  ))}
</Box>

## Shape

<Box>
  <Row label="Regular">
    <Avatar size="lg" />
  </Row>
  <Row label="NFT">
    <Avatar nft size="lg" />
  </Row>
</Box>
