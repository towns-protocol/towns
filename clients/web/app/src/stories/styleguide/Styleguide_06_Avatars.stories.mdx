import { Meta, Canvas, Story } from "@storybook/addon-docs";
import {
  Avatar,
  Box,
  Icon,
  Grid,
  Text,
  Paragraph,
  Heading,
} from "../../ui/components";
import { avatarSizes } from "../../ui/components/Avatar/Avatar.css";
import { vars } from "../../ui/styles/vars.css";
import {
  flexAlignment,
  flexJustifyAlignment,
} from "../../ui/styles/atoms/sprinkles.css";
import { Row, Square } from "../StoryUtils";

<Meta title="Style Guide/Avatars" />

# Avatars

## Sizes

<Box>
  {Object.keys(avatarSizes).map((c) => (
    <Row key={c} label={c}>
      <Avatar size={c} />
    </Row>
  ))}
</Box>

## Shape

<Box>
  <Row label={`Regular`}>
    <Avatar size="lg" />
  </Row>
  <Row label={`NFT`}>
    <Avatar size="lg" nft />
  </Row>
</Box>
