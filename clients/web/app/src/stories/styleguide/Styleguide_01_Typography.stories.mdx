import { Canvas, Meta } from "@storybook/addon-docs";
import { Box, Heading, Paragraph, Text } from "@ui";
import { vars } from "../../ui/styles/vars.css";
import { Row } from "../StoryUtils";

<Meta title="Style Guide/Typography" />

# Typography

## Paragraphs

<Box>
  <Row label="Default">
    <Paragraph>Default</Paragraph>
  </Row>
  {["sm", "md", "lg", "xl"].reverse().map((c) => (
    <Row key={c} label={c}>
      <Paragraph size={c}>Paragraph {c}</Paragraph>
    </Row>
  ))}
</Box>

## Headings

<Box>
  <Row label="Default">
    <Heading>Heading Default</Heading>
  </Row>
  <Row label="level 1">
    <Heading level="1">Heading level 1</Heading>
  </Row>
  <Row label="level 2">
    <Heading level="2">Heading level 2</Heading>
  </Row>
  <Row label="level 3">
    <Heading level="3">Heading level 3</Heading>
  </Row>
  <Row label="level 4">
    <Heading level="4">Heading level 4</Heading>
  </Row>
</Box>

## Colors

<Box>
  {Object.keys(vars.color.text).map((c) => (
    <Row key={c} label={c}>
      <Paragraph color={c}>{c}</Paragraph>
    </Row>
  ))}
</Box>

## Font weights

<Box>
  {Object.keys(vars.fontWeight).map((c) => (
    <Row key={c} label={c}>
      <Paragraph fontWeight={c}>Text font weight {c}</Paragraph>
    </Row>
  ))}
</Box>

## Examples

### Paragraph

<Canvas isColumn withSource="open">
  <Paragraph color="accent" size="lg" textAlign="center">
    Paragraph content
  </Paragraph>
</Canvas>

### Heading

<Canvas isColumn withSource="open">
  <Heading color="accent" level="1">
    Heading content
  </Heading>
</Canvas>

### Text Primitive

<Canvas isColumn withSource="open">
  <Text as="span" color="muted1" size="lg" textTransform="uppercase">
    Custom Text
  </Text>
</Canvas>
