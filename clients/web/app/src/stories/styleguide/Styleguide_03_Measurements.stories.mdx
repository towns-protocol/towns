import { Meta } from "@storybook/addon-docs";
import { Box } from "../../ui/components";
import { vars } from "../../ui/styles/vars.css";
import { Row, Square } from "../StoryUtils";

<Meta title="Style Guide/Measurements" />

# Measurements

## Box / Height

<Box>
  {Object.keys(vars.dims.height).map((c) => (
    <Row key={c} label={c}>
      <Box height={c} border="strong" />
    </Row>
  ))}
</Box>

## Square / Width x Height

<Box>
  {Object.keys(vars.dims.square).map((c) => (
    <Row key={c} label={c}>
      <Box square={c} border="strong" />
    </Row>
  ))}
</Box>

## Box / Gap

<Box>
  {Object.keys(vars.space).map((c) => (
    <Row key={c} label={c}>
      <Box direction="row" alignItems="center" gap={c}>
        <Box
          background="inverted"
          height="height_md"
          style={{ width: vars.space[c] }}
        />
      </Box>
    </Row>
  ))}
</Box>

## Box / Padding

<Box>
  {Object.keys(vars.space).map((c) => (
    <Row key={c} label={c}>
      <Box
        direction="row"
        alignItems="center"
        padding={c}
        color="default"
        border="strong"
      >
        <Square />
      </Box>
    </Row>
  ))}
</Box>
