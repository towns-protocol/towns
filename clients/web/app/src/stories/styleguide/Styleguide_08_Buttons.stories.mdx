import { Meta } from "@storybook/addon-docs";
import { vars } from "../../ui/styles/vars.css";
import { Box, Button, Stack } from "../../ui/components";
import { Row } from "../StoryUtils";

<Meta title="Style Guide/Buttons" />

# Buttons

## Sizes

<Box>
  {Object.keys(vars.dims.button).map((c) => (
    <Row key={c} label={c}>
      <Stack horizontal>
        <Button icon="settings" size={c}>
          Action
        </Button>
      </Stack>
    </Row>
  ))}
</Box>

## Content

<Box>
  <Row label="text only">
    <Stack horizontal>
      <Button>Action</Button>
    </Stack>
  </Row>
  <Row label="icon only">
    <Stack horizontal>
      <Button icon="bell" variant="warning" />
    </Stack>
  </Row>
  <Row label="text + icon">
    <Stack horizontal>
      <Button icon="check" variant="positive">
        Done
      </Button>
    </Stack>
  </Row>
</Box>

## Tones

<Box>
  {Object.keys(vars.color.background).map((c) => (
    <Row key={c} label={c}>
      <Stack horizontal>
        <Button icon="settings" tone={c}>
          Action
        </Button>
      </Stack>
    </Row>
  ))}
</Box>
